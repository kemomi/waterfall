{
    "name": "4",
    "version": "1.0.0",
    "scripts": {
      "clean": "rimraf dist && mkdir dist",
      "build": "npm run clean && babel src --out-dir dist",
      "prebuild": "npm test",
      "test": "jest"
    },
    "devDependencies": {
      "babel-cli": "^6.0.0",
      "jest": "^24.0.0",
      "rimraf": "^3.0.0"
    }
  }
  